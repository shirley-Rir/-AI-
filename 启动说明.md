# 智能音乐推荐系统启动说明

## 快速启动

### Windows系统

1. 双击运行 `start.bat` 文件，系统将自动启动所有服务
2. 等待所有服务启动完成，浏览器会自动打开系统页面

### 手动启动（适用于所有系统）

#### 1. 启动音乐服务（需要先启动依赖参考[NeteaseCloudMusicApiEnhanced](https://github.com/neteasecloudmusicapienhanced/api-enhanced)）

```bash
# 进入音乐服务目录
cd backend/music-service

# 安装依赖
npm install

# 启动服务（默认端口3001）
npm start
```

#### 2. 启动AI服务

```bash
# 进入AI服务目录
cd backend/ai-service

# 安装依赖
pip install -r requirements.txt

# 启动服务（默认端口5000）
python app.py
```

#### 3. 启动前端

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动开发服务器（默认端口8080）
npm run serve
```

## 访问系统

启动完成后，在浏览器中访问：`http://localhost:8080`

## 常见问题

### 1. 端口冲突

如果默认端口被占用，可以修改各服务的端口：

- 音乐服务：修改 `backend/music-service/.env` 文件中的 `PORT` 值
- AI服务：修改 `backend/ai-service/app.py` 文件最后一行的 `port` 参数
- 前端：修改 `frontend/.env.development` 文件中的 `VUE_APP_API_BASE_URL` 值

### 2. 依赖安装失败

- Node.js依赖：确保已安装Node.js（版本 >= 14）
- Python依赖：确保已安装Python（版本 >= 3.8）和pip

### 3. ESLint错误

如果前端启动时出现ESLint相关错误：
- 已添加ESLint配置文件(.eslintrc.js)，应该能解决大部分ESLint错误
- 如果仍有问题，可以临时禁用ESLint检查：在vue.config.js中设置`lintOnSave: false`
- 或者使用命令行参数启动：`npm run serve -- --skip-plugins @vue/cli-plugin-eslint`

### 4. 网易云音乐API限制

如果遇到网易云音乐API访问限制，可以：

1. 在 `backend/music-service/.env` 文件中设置 `NETEASE_COOKIE`（需要网易云账号）
2. 修改 `NETEASE_IP` 为其他中国IP地址

### 5. 通义千问API配置

在 `backend/ai-service/.env` 文件中设置有效的 `QWEN_API_KEY`，否则AI分析功能将无法正常工作

## 测试API

项目提供了测试脚本，可以帮助您验证各服务的API是否正常工作：

1. 启动音乐服务和AI服务
2. 运行测试脚本：
   ```bash
   # 进入测试目录
   cd test

   # 运行测试
   run-tests.bat
   ```

测试脚本会测试以下API：
- 获取歌曲URL
- 搜索歌曲
- 获取歌曲详情
- 文本推荐
- 获取历史记录

## 查看日志

如果遇到歌曲信息显示不完整的问题，可以通过查看日志来诊断：

1. **前端日志**：
   - 打开浏览器开发者工具（F12）
   - 在控制台(Console)中查看日志输出
   - 查找"获取到的文本推荐歌曲数据"或"获取到的图片推荐歌曲数据"

2. **音乐服务日志**：
   - 查看音乐服务控制台输出
   - 查找"搜索API返回结果"和"歌曲详情API返回结果"

3. **AI服务日志**：
   - 查看AI服务控制台输出
   - 查找"搜索到的歌曲"和"获取到的歌曲详情"

这些日志将帮助您了解数据在哪个环节丢失或格式不正确。

## 开发模式

如需在开发模式下运行（支持热重载），可以使用以下命令：

- 音乐服务：`npm run dev`
- AI服务：使用支持热重载的Python服务器，如Flask-Script
- 前端：`npm run serve`（默认已启用热重载）
